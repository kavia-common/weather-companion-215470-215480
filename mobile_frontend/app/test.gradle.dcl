testing {
    // Disable failure when no tests are discovered by Gradle runner in CI
    failOnNoDiscoveredTests = false

    // Ensure JUnit Platform (Jupiter) is used for discovery
    useJUnitPlatform = true
}
